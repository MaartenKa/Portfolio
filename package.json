{
  "name": "qa-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal QA and Test Automation portfolio",
  "scripts": {
    "build": "tsc",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "typescript": "^5.9.0"
  }
}
